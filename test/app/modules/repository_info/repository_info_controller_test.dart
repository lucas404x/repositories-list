import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:repositories_list/app/modules/repository_info/repository_info_controller.dart';
import 'package:repositories_list/app/modules/repository_info/repository_info_module.dart';

void main() {
  initModule(RepositoryInfoModule());
  // RepositoryInfoController repositoryinfo;
  //
  setUp(() {
    //     repositoryinfo = RepositoryInfoModule.to.get<RepositoryInfoController>();
  });

  group('RepositoryInfoController Test', () {
    //   test("First Test", () {
    //     expect(repositoryinfo, isInstanceOf<RepositoryInfoController>());
    //   });

    //   test("Set Value", () {
    //     expect(repositoryinfo.value, equals(0));
    //     repositoryinfo.increment();
    //     expect(repositoryinfo.value, equals(1));
    //   });
  });
}
