class RepositoryModel {
  String name;
  String description;
  int stars;
  int forks;
  int watchers;

  RepositoryModel(
      {this.name, this.description, this.stars, this.forks, this.watchers});
}
